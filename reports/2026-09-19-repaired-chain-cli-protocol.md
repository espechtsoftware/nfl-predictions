# Complete repaired-chain CLI rehearsal, bounded and outcome-free

Freeze before comparative generation or selection. This follows the isolated cache protocol and requires both scratch caches to pass support, finite/ordered quantile and source-stability checks. Purpose: test the actual live pipeline with repaired features, refreshed marginal cache and repaired production projections, and locate changes through candidate generation and selection. It is not a money-dose performance claim or a real-outcome experiment.

## Two arms and fixed implementation

Both arms execute the full `scripts/live_week.py` from lab `2dc116c` (actual game-input fix `40a9be9`), including explicit current game inputs, original eligibility/level/identity/legality guards, generation and dual-law selection. Control reads `nfl_features_control` / `nfl_predictions_control`; repair reads `nfl_features_salaryfix` / `nfl_predictions_salaryfix`. Training through 2025 must be common and verified equal after stable key ordering. Hsim fits retain the historical benchmark and outcome cutoff2026. No target-week outcome value can be read.

All non-arm warehouse inputs are frozen once and replayed identically: salary, roster/status/injury records, current schedules and props. Each arm's crosswalk, inference features, TabPFN cache and production projections are retained with exact query text, table metadata, query job and file hashes. The router permits only the enumerated SELECT consumers, refuses unexpected tables/queries, and never issues DML or load jobs. Cached DataFrames are copied before the caller mutates them. The actual CLI source stays unchanged; an additional adapter receipt records the read routing and code identity alongside its original receipt.

Use target2026/week2/group153428, **32 leverage +128 boom candidates**, **97 entries**, **10,000 worlds**, K1 and seed2026; equal candidate budget and selector in both arms. No low-level, dirty-tree or status override. One local process/one CPU at a time, 600-second cap per arm; if a cap or contract fails, record it and reassess without silently changing the experiment. A bounded small-dose rehearsal does not validate production-dose runtime or prove its selected book is optimal.

## Retained worlds and audit

Retain generation, incumbent selection and incumbent independent-audit player banks directly from the real CLI's returned globals, with exact float32 score summation. Retain hsim selection. Recompute the existing calibration at the fixed selection seed and assert its final selection sample equals the CLI's hsim bank exactly; then draw a separate final audit at seed2426 using those same calibration weights. Audit worlds never feed candidate generation or selection. No blend, seed, threshold or objective search.

Report frame identity/feature/projection differences, common-key marginal means/tails, candidate overlap/novelty, selected membership/order, first lineup identity, eligibility/legality results and execution timing. Evaluate each fixed book under both arms' independent I and H audits, and their equal-weight mixture: expected maximum, P220 and GLOBAL proxy at prefixes1/10/20/30/40/80/90/97 plus the twelve actual contest blocks. If player universes differ, report that explicitly; do not assign missing cross-arm scores zero or quietly restrict books. Only common fully scoreable books receive cross-law comparisons.

Primary descriptive contrast is repair minus control under each evaluation law at K97. Publish disagreement as well as gains, with conditional paired Monte Carlo errors. No real football success probability or return is inferred. Fixed-pool input factorial results remain separate from this complete-chain generation comparison.

No timer, production job configuration, entry allocation, stakes or upload changes. No bank991 or current lineup outcome read. All new reports receive main-project reading copies and committed/pushed peer handoffs.
