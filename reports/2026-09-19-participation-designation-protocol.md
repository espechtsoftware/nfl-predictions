# Which designation drives the participation-selection result?

This follow-up is exploratory and motivated by the completed current-pool P_MIX comparison. It asks whether the selection gain mainly comes from doubtful-player nonparticipation, and measures what is lost or gained when questionable-player uncertainty is omitted. It is not a new historical efficacy claim or an adoption gate invented after observing a result.

Use the same frozen repaired D1600 candidate pool, K97, score laws, original selection banks, fixed prior-season map and11:50status snapshot as the original test. The new provider-bound12:42capture has independently reproduced identical probabilities and selections; it does not retroactively change that original cutoff. No source/model/weight/threshold/prefix/dose search.

Select four books: ordinary control; full prior participation mixture; doubtful-only mixture (Q set to1); questionable-only mixture (D set to1). Full mixture must reproduce the original selected order exactly. Use the original selection-mask seed20260919054 and derive each narrower mask from that same per-player full-mixture draw, so unrelated random draws do not change when a status class is omitted. All books use the identical legal candidate pool and exactly97members. Apply the same actual vetting helper and captured inputs to each; report raw and delivered order explicitly.

Generate **fresh component audit banks**, incumbent seed12260919 and corrected-hsim seed13260919, with10,000draws each. Reconstruct the original fixed fitted laws and prove bitwise equality of original banks before drawing anything new; retain the generation adapter and receipts. These audit banks are not used for selection. Availability seed20260919055 is also fresh. Evaluate every book under all-active, doubtful-only, questionable-only and full availability assumptions, using common masks by class. These are sensitivity laws, not observed game-day states.

Report both components and their equal mixture, expected maximum, P220, fixed winner proxy and all existing metrics/contest blocks/prefixes. Primary interpretation is doubtful-only versus full-mixture selection under full-mixture and doubtful-only audit assumptions; all other contrasts remain visible. Monte Carlo intervals measure finite-draw uncertainty for these fixed assumptions, not predictive validity or uncertainty in the learned map. New narrower rules have no independent historical adoption evidence merely because full P_MIX had a positive prior study.

Run a synthetic complete selection/delivery/audit smoke and real artifact support checks before freeze. One heavy local process at a time;600second cap per bank/reader command. Preserve failures; no current-season scoring data or bank991 query. No live settings, entry files, contest allocation, images, timers or historical cache changes.
