# Usage-only hsim sensitivity on the archived Week2 pool

Design recorded before running the repaired-input simulation. This is a mechanistic sensitivity study, informed by the already observed missing-history defect and five receivers' recovered usage; it is not a confirmatory efficacy test.

Use the authenticated Thursday D6400 candidate/frame archive and the original incumbent/hsim score banks. The exact hsim replay already passed at frozen `b9392eb0`, result `bd98a27e`: all 4.35 million scores match. The control uses that exact archived frame. The treatment keeps every identity, roster, projected mean, schedule/law input, depth rank, cold-start flag and other frame column fixed. Replace exactly six strict-prior usage columns, joined by GSIS id for season2026/week2: `snap_share_l4`, `target_share_l4`, `carry_share_l4`, `games_played_prior`, `rz20_target_share_l4`, `gl3_carry_share_l4`. Use a read-only, identity-recorded export from the independently validated scratch feature/usage table after the workstation's isolated build. Missing identities retain archived values and are enumerated. No inference from a missing row to a zero. Do not use Week2 actuals.

Require unique source GSIS ids and source season/week, finite-or-null shares, shares in [0,1], prior-games in {0,1}, exactly 435 unchanged frame identities and exact equality of every non-treatment frame column. Save the query, job id, source table metadata, retrieved time and canonical rows/hash before simulation. Freeze the exact export identity in a committed supplement before reading simulation effects. Full repaired-pipeline projection changes are a separate workstation experiment.

Run unchanged hsim at seed2326 with 10,000 worlds for the treatment. Preserve the original five calibration iterations and all production rules. Build float32 candidate totals in original roster order, matching existing source summation. Reproduce the original97 selection under equal-mass incumbent+original-hsim worlds, using the verified chunked float64 greedy expected-max implementation. Require all97 selected indices equal the previously frozen diagnostic extension. Then perform one unchanged greedy selection under incumbent+repaired-usage-hsim worlds. No search over seeds, weights, thresholds, iterations or selector families.

Measure player activity/positive-weight transitions, all-player and selected-player mean deltas, zero-score rows introduced/removed, by-position and largest absolute mean differences, selected97 membership/order overlap, first-lineup identity, and per-player exposure change. Report original and new books evaluated under each original component and repaired-usage hsim: expected max, P(>=220), and the previously frozen GLOBAL winner utility proxy (48 constants, bandwidth8). Also report prefixes1/10/20/30/40/80/90/97 and the actual12 contest blocks. These reused selection worlds are optimistic diagnostics.

As a separate sensitivity check, simulate both control and treatment at fixed seed2426,10,000 worlds each and evaluate both already-fixed books. These are independent **hsim-half** audit worlds only; the incumbent independent audit bank is unavailable, so never call this a full dual-law audit. Publish all four book/law combinations without choosing the favorable one. A lower modeled maximum after removing false opportunity is not automatically worse real-world performance, and a higher one is not proof of improvement.

Preserve all player score arrays locally with hashes; retain script, input receipts and result summaries in the report repository. One process/one BLAS thread, 420-second cap, arrays planned below2GiB. No regenerated candidate pool, saved entry changes, live model/data writes, paid vendor requests or bank990/991 reads. Any guard failure stops and is documented before an amended run.
