# Reconciliation: pass-tail breadth and selector resampling feedback

Date: 2026-08-14. This reconciles
`reports/2026-08-14-pass-tail-and-selector-resampling-feedback.md` without
changing either frozen decision.

## Accepted

- Bootstrap-mean bagging is closed as algebraically redundant. Do not build a
  sample-splitting or shrinkage selector unless a new protocol and genuinely
  new evidence justify it. The completed multi-seed factorial is the stronger
  new-world experiment.
- Disjoint-half overlap (`54.28/80`) now has equal billing with bootstrap
  overlap (`61.64/80`) in the selector result and final forensic register.
- Exact-80 comparisons must separate nested tail totals from gross threshold
  crossings and distinct calendar slates. A reusable diagnostic and regression
  test now implement that contract, and the pass-tail result is retrofitted
  descriptively from its unchanged frozen panels.
- The 2025 pass-tail weakness is an explicit prospective 2026 finite-K shadow
  question at Weeks 4, 8, 13 and 18. It cannot silently change the K=1 money
  policy.
- The pass-tail cache/schedule transfer boundary remains intact.

## Qualification

The `54.28/80` disjoint-half overlap does **not** imply “54 effective entries.”
It measures which rosters are reproduced under different simulated evidence;
it does not show that the other 26 entries have zero portfolio or payout
value. Effective entry count, marginal value of entries 41--80 and ROI require
nested pre-lock books plus contest payouts/duplication data and belong in the
registered final forensic analysis.

## Pass-tail breadth result

At the deciding 220 line there are three improving seed/slate crossings and
one worsening crossing, for the registered net `+2`. They occur on only two
calendar slates: `2023-W03` and `2024-W03`; `2023-W03` improves in R0/R1 but
worsens in R4. Across every threshold, 19 unique seed/slates improve and 15
worsen, spanning 14 distinct improving slates, 14 distinct worsening slates
and 23 changed slates. The treatment remains selected by the frozen law, but
the evidence is correctly described as modest-breadth rather than eight
independent threshold gains.
