# PIT-clean TabPFN SCHED exact-80 addendum

Frozen 2026-08-12 before either SCHED cache exists and before any SCHED cache
prediction, final-served metric, candidate, or lineup score is visible. It
implements the already-frozen SCHED feature-sync protocol on the repaired
terminal lineage and applies the operator's current program-wide tail-first
law.

## Terminal branches

- Both cache arms inherit the exact terminal active-label context law. Their
  newly generated 33-feature control must reproduce the inherited selected
  cache on every key and prediction before any gate is eligible.
- A failed SCHED final-served gate closes this feature pair without lineup
  generation and retains the incumbent feature/cache law.
- A passing final-served gate licenses exactly one paired exact-80 comparison
  using fixed panels
  `20260812-pitclean-e80-selected-tabpfn-sched-control-v1` and
  `20260812-pitclean-e80-selected-tabpfn-sched-treatment-v1`.
- An exact-80 loss or exact tie retains the incumbent feature/cache law. An
  exact-80 win selects the treatment's 35-column SCHED law for the later
  team-QB-quality branch. Neither outcome directly mutates production.

## Common law and only difference

Both books generate only 2023--2025 and splice the unchanged terminal Tier-1
2019/2021/2022 history into their 107-slate comparison. They share the
terminal K1, direct-role, multinomial/finite-K, active-label context law,
exact-80 selection, candidate budget, seeds, market blend and all optimizer
settings. Each arm uses its own unrounded walk-forward position-factor
schedule from the sole passing SCHED final-served report.

Control uses `tabpfn_sched_control_v1`; treatment uses
`tabpfn_sched_treatment_v1`. The treatment cache's appended
`net_rest_diff,body_clock_hour` contract and the independently fitted schedule
it causes are the only arm differences. The comparator must prove exact player
keys, invariant point-in-time inputs, actuals, seeds and common levers; exactly
the seven registered distribution-derived player outputs may change. Candidate
means/membership and selected lineups must change, and every completed slate
must contain 80 distinct legal selections.

## Decision

Compare full-book selected weekly maxima in order
`240,230,220,210,200,194,187`. The first nonzero treatment-minus-control
count decides. If all seven counts tie, mean weekly maximum decides; an exact
tie retains the incumbent. Season signs and average lineup scores are
diagnostics only. This supersedes the older five-threshold wording before any
SCHED outcome exists and is identical to the repaired Tier-1, position, usage,
and active-label operator law.
