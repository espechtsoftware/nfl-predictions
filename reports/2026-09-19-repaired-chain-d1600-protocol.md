# Expanded1600-candidate co-run and prior selection sensitivity

The160-candidate full-chain rehearsal passes, but97entries consume most of that pool. The two historical target priors restore support while changing only two selected memberships. This follow-up changes the declared candidate budget to **320leverage+1280boom**, keeping97entries,10000worlds,K1,seed2026, original generation profiles and selectors. Run control and salary repair once each, serially; cap each at600seconds on one CPU. A cap failure is recorded rather than silently changing the budget. This is a larger integration/selection sensitivity, not the operational12560-candidate dose.

All warehouse reads replay the existing immutable complete-chain snapshots, **with refusal of any missing snapshot or live query**. No newer salary/status/market/data pull enters either arm. The live CLI source stays2dc116c and all guards remain active. Each arm's saved frame and all five player banks must be exactly identical to its160-candidate counterpart; the new budget changes candidate generation, not player forecasts or either selection/audit law. Preserve original ordered roster identities and candidate ledger. Do not assume the small pool is a nested subset because leverage enumeration may depend on the requested dose.

Repeat the complete-chain comparison once using the same frozen definitions, all component cross-audits/mixtures, ordinary and WEMAXbooks, prefixes and actual contestblocks. Preserve missing-support markers, pin RangeIndex/candidate-number alignment and record GLOBALproxy bandwidth8 explicitly, following peer review7fbeb75. Publish all results, including losses, with conditional uncertainty and no real-NFL efficacy inference.

Then use the repaired expanded pool to compare baseline, one-prior-game and past-empirical EMAX/WEMAXselections using **the already-retained player banks from the calibrated prior trace**. The exact frame/bank parity gate above makes re-simulation unnecessary. Priors, calibration weights, seeds, I/H mixture weights and metrics remain frozen. No new model fitting, prior choice, swaps or parameter search. Report whether more selection room changes the previously tiny effect and whether first-lineup/contest tradeoffs survive. Distinguish this from the complete-chain control-versus-repair comparison.

Freeze both equal-budget control/repair books before the slate and retain them for actual-point settlement regardless of the eventual entered system. Entry is not required to observe counterfactual lineup points. One prospective slate is one observation, not a conclusive validation or rejection.

The source adapters are review artifacts only; no live project configuration, timers, entry mix, paid-data schedules or DraftKings upload changes. No current-year outcome labels or bank991 are read. This work proceeds alongside release/rollback preparation within the Saturday15:30UTCbuild window.
