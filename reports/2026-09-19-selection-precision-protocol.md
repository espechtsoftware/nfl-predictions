# Selection precision: five fixed-law batches and a fresh audit

The direct 220-objective screen lost its apparent tail benefit on independent worlds. This follow-up tests finite-simulation precision without treating that explanation as established.

Keep the repaired D1600 candidates, original ordered rosters, frame, served means, component fits, hsim calibration, game inputs and equal I/H weights fixed. The initial selection batch is exactly the original 10,000 draws per component. Add **four independent batches of 10,000 per component**, for 50,000 per component / 100,000 mixture draws. Incumbent extra seeds are 6202601–6202604; hsim extra seeds 7202601–7202604. Every incumbent batch uses the unchanged full marginal construction and per-bank centering. Hsim uses the saved calibration, with no new pilots or refit.

Compare the three previously frozen 97-entry orders (ordinary EMAX, coverage220, excess220) with those same three algorithms selected on the enlarged nested draw set. No threshold, weight, prior, candidate-dose or hyperparameter search. Exact coverage tie order and excess transform remain those at `f16ad722`. Compare each enlarged book with its corresponding original book and with original EMAX.

**Primary audit is fresh:** incumbent seed 8202601, hsim seed 9202601, 10,000 draws each, with the same fixed fits/calibration. These draws are not used in selection. The previously inspected old/control and repaired audits are secondary descriptive cross-checks only. All expected-maximum, P220, GLOBAL proxy, P194/P240 and excess220 metrics, paired Monte Carlo uncertainty, prefixes and contest blocks are reported. No stopping after the first favorable metric or choosing a seed after reading results.

Mechanics: obtain the incumbent generator from an unchanged small full CLI replay at clean lab `2dc116c`, with all warehouse queries forced to the existing immutable snapshots and provider queries forbidden. Authenticate all four benchmark tables. Require the complete frame, original generation/I-selection/I-audit/H-selection banks, exact fixed-calibration H-audit, and a second call of the captured incumbent function at seed2076 to reproduce the original artifacts before adding draws. Save every new bank and its identity before reading comparative audit results. First execute an input-replay/bank-construction phase; then execute the frozen selection/audit reader on those retained banks. Each phase has a 600-second cap, one CPU. Record any cap/failure without silently extending it.

This is a conditional simulation experiment, not historical NFL validation. Changing the live `--sims` option would also change generation and is **not** equivalent to this fixed-pool study. Any operational follow-up would need a separate selection-draw option and appropriate historical/integration checks. No live adoption, new data pull, current outcomes or bank991.
