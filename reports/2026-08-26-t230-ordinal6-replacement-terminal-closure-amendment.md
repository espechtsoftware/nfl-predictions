# T230 ordinal-6 replacement terminal-closure amendment

Date frozen: 2026-08-26, after the sole authorized ordinal-6 attempt-1 worker
became terminal and before any recovery-terminal or supplemental Lane-A
closure was published, and before any result, acceptance, realized outcome,
or score was read.

This amendment is subordinate to and preserves the complete history in
`2026-08-26-t230-ordinal6-bounded-platform-replacement-amendment.md` and its
two correction addenda.  It grants no new execution.  It defines only the
fail-closed terminal receipts needed to make the exhausted one-worker law
durable and unambiguous.

## Terminal fact and controlling law

The exact reviewed first-creator controller submitted the one permitted
attempt-1 worker as Cloud Run execution
`atlas-minimal-c-s2023-w1-v1-67669`.  It completed at
`2026-08-26T05:18:35.473406Z` with `Completed=False`, reason
`NonZeroExitCode`, one failed task, zero successful tasks, and a task message
reporting exit code 1/container error.  It started at
`2026-08-26T05:15:51.761279Z`.

The original amendment states that if the replacement fails for any reason,
ordinal 6 and the full panel are terminal-invalid and there is no second
replacement.  This closure does not reinterpret that rule, classify the
application error, or create another exception.  It records the exhausted
state so no later process can mistake missing success objects for an
unconsumed launch.

## Frozen launch lineage

The closure must exact-replay these already-created objects by exact name and
generation-pinned content:

1. replacement intent at generation `1787721338174308`, SHA-256
   `86f34d3c755b68a925e354c7379c1e6c54b7e4856b2dfc632009cc84de45133d`,
   85,600 bytes;
2. launch ownership at generation `1787721341183601`, SHA-256
   `15f0881fe9254bd765e2ed6278a6dd71b88ed579818b1d5734601b9256784fdf`,
   145,712 bytes; and
3. attempt-1 replacement stage start at generation `1787721341713255`,
   SHA-256
   `468e278eb078395f6ebef9479a72f01247834b1d56a9aeb1a790662c4ef2cbb0`,
   192,151 bytes.

The intent must replay through the committed ordinal-6 review lock and exact
sealed implementation.  Ownership must embed that exact intent, bind exactly
one consumed submission, name the replacement execution, and retain the
frozen D2 image/job/resources/flags/runtime payload.  The stage start must
embed that exact ownership and bind runtime attempt 1 and the same execution.

## Exact terminal projection

Before any closure publication, a reviewed controller must issue only these
two direct-known reads:

- an exact Cloud Run execution describe for
  `atlas-minimal-c-s2023-w1-v1-67669`; and
- an execution-scoped task query that returns exactly one task with the exact
  expected task name, job, location, and execution labels.

The projection must bind the raw describe SHA-256 and byte length, the exact
known execution-status key surface, the complete task key surfaces, terminal
condition status/reason/message, completion/start times,
failed/succeeded/cancelled count presence and values, and the task-attempt
surface.  Separately, it must replay the full submitted
envelope/environment from the execution and prove equality to the reviewed
launch ownership submitted-execution projection, including frozen D2,
service account, 8 CPU, 32 GiB, one task, parallelism 1, 21,600-second
timeout, `maxRetries=0`, in-memory evidence mount, exact command, literal
runtime payload, and exact configured environment.  This law does not claim
that unretained execution top-level, metadata, or spec key sets were frozen.

The final task-row literals are not inferred from the execution summary.
They must be frozen from the exact task query before static seal.  Any absent,
extra, or changed field, different reason/message, success, cancellation,
retry, timeout, platform code 13, memory failure, or ambiguous task query
fails closed.  This amendment never upgrades a failed task to success merely
because some output object exists.
The task `metadata.name` must itself equal
`atlas-minimal-c-s2023-w1-v1-67669-task0`; a controller may not substitute a
hard-coded expected name into the projection without validating the returned
name first.

## Outcome-blind absence and presence census

The closure controller may inspect mechanics receipts and exact-name object
metadata only.  It must never download or parse a science result or
acceptance body.

Immediately before the recovery terminal is created, and again immediately
before the Lane-A closure is created, it must exact-name probe:

- the deterministic result URI;
- attempt-1 worker runtime URI;
- canonical run-slate stage URI;
- worker amendment and success-completion URIs;
- every verifier-6 request/journal, ownership, start, runtime, stage,
  acceptance, terminal, and completion URI;
- the supplemental Lane-A root URI; and
- every supplemental/joint panel root URI reserved by the ordinal-6 law.

The post-failure exact-name census established that the deterministic result,
attempt-1 runtime, and canonical worker stage are all absent.  Every listed
surface must remain absent in both closure passes and each row records
`content_inspected=false`.  A later presence, even if equal-looking, changes
the frozen terminal surface and requires a separately reviewed amendment; it
does not rescue the failed execution or authorize a bridge.  An ambiguous
metadata response is terminal.
No bucket listing, prefix scan, latest alias, log search, result read,
acceptance read, effect read, realized-outcome read, or scoring operation is
allowed.

## Recovery execution terminal

After exact lineage, terminal, and census replay, one create-once recovery
terminal may be published at the already-reserved
`platform-replacements/run-slate/06/attempt-01/execution-terminal-v1.json`
URI.  It must bind:

- all three launch-lineage identities and their validated self-hashes, with
  the generation-pinned bodies required to exact-replay before construction;
- the exact replacement execution name and terminal projection;
- both ordered metadata-only surface censuses;
- the original failed execution and attempt-0 mechanics identities;
- the frozen replacement contract, amendment, correction addenda, review
  lock, and clean committed implementation identity;
- `replacement_worker_execution_count=1` and
  `replacement_worker_execution_limit=1`;
- `request_consumed=true`, `replacement_failed=true`,
  `replacement_exhausted=true`, `second_replacement_allowed=false`, and
  `bridge_verifier_allowed=false`; and
- a complete false-authority closure.

The terminal is create-once.  An equal existing object is resolve-only.  An
unequal collision, create ambiguity, or failure to generation-pin the created
bytes leaves the panel terminal-invalid without permission to retry or
publish a substitute URI.

## Supplemental Lane-A terminal-invalid obligation/root

After the recovery terminal exists and is generation-pinned, a separate
create-once Lane-A terminal-invalid root may be published at the reserved
supplemental Lane-A URI.  It is a negative obligation, not an accepted lane
ledger.

It binds exact ordered identities for the completed ordinal-0 through
ordinal-5 worker/verifier stages and acceptances, the ordinal-5 predecessor,
the complete ordinal-6 primary/replacement failure chain, and the recovery
terminal.  It declares ordinals 6 through 27 incomplete, canonical Lane-A
ledger absent, Lane-A accepted count 6 of required 28, and the full T230 panel
invalid.  It must not fabricate placeholders for ordinals 6 through 27,
claim 28 acceptances, or depend on Lane B or a core panel release.

The root is the exact object a future audit or replacement panel plan may
bind to prove why this run stopped.  It cannot be used as a predecessor for
ordinal 7, a verifier request, a canonical lane ledger, a panel finalizer,
R6, or scoring.

The original immediate supplemental joint-panel-root assumption is
superseded by this negative closure.  No joint root is published for this
run.  The independent ordinal-35 failure may receive its own terminal closure,
but combining two invalid lanes cannot make this panel accepted.

## Implementation and review boundary

The terminal-closure contract, controller, and focused tests must live in new
files and must not modify the sealed ordinal-6 replacement implementation.
Before either closure object is published:

1. freeze the exact implementation, test, and amendment bytes as one static
   review candidate;
2. obtain independent static P0/P1/P2 disposition over those exact bytes;
3. run exactly one focused offline test invocation authorized by that review
   and retain its exact tracked output;
4. run exactly one real-artifact, outcome-blind preflight that performs the
   exact terminal/lineage/metadata census but creates no cloud object, and
   have its create-once tracked receipt bind the same reviewed implementation
   and amendment measurements; and
5. bind the successful test and preflight receipts, exact commands/counts,
   static disposition, and unchanged current bytes in one final tracked lock
   at a clean committed HEAD.

The preflight is consumed before any backend/client construction or cloud
read.  Its CLI must first create and fsync a fixed-path tracked attempt marker
that binds the exact reviewed implementation commit and measurements.  A
read failure, process crash, partial run, or missing final receipt still
consumes the one attempt forever.  An existing file, final/intervening
symlink, write ambiguity, or unequal marker is terminal; the marker is never
deleted or reused.  A successful preflight receipt and the final review lock
must both bind the exact marker bytes and self-hash.

The final lock is built deterministically by the reviewed CLI after the test
and preflight.  It binds an earlier clean implementation-source commit, not
the impossible hash of the later commit that contains the lock itself.  At
publication, the later clean HEAD must contain the exact lock, marker,
preflight, test output, amendments, and unchanged implementation bytes, while
the implementation bytes must also exact-replay from the earlier reviewed
commit.  The lock must never require its containing HEAD to equal its own
embedded implementation commit.

A failed test or preflight consumes its allowance unless a new pre-publication
correction addendum truthfully records and independently authorizes a bounded
correction.  The preflight permits Cloud reads only.  It permits zero GCS
publication, job mutation, Cloud Run submission, result/acceptance body read,
outcome read, graph mutation, or score.

## Consequence boundary

This amendment authorizes no execution.  All recovery-terminal and Lane-A
closure fields for automatic retry, bridge verifier, lane resume, canonical
lane acceptance, panel release, amended panel acceptance, realized outcomes,
historical scoring, corpus fill, selection, graph mutation, live policy,
production change, analytical authority, R6 freeze, promotion, and decision
remain false.

The safe consequence is explicit: the current T230 run cannot produce the
full-system historical scores it was intended to unlock.  Any future score
requires a separately designed and pre-frozen new panel run; it cannot reuse,
delete, overwrite, or conceal either consumed ordinal-6 execution.
