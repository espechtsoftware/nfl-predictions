# ATLAS matched-diversity MVP resource-only repair3 protocol

Date frozen: 2026-08-16, after strict harvest of the native and cgroup
diagnostics, while the prospective 16-GiB preflight remained nonterminal and
before any preflight artifact was opened.
Run ID: `20260816-atlas-matched-diversity-mvp-v1-repair3`

## Evidence and purpose

The cgroup diagnostic returned exact SIGKILL for 2024 Weeks 7 and 16 and a
successful Week 15 R0. All three cells peaked above the prospectively frozen
80% pressure boundary at 4 GiB. OOM is not directly proven because Cloud Run
exposed cgroup-v1 `failcnt` rather than `oom_kill`, but resource pressure is
supported and the frozen 16-GiB branch is licensed.

Repair3 asks only whether the original ATLAS score-free MVP can complete and
pass its original mechanical/effect gate under that resource envelope. It is
not a new ATLAS mechanism and may not change the scientific calculation.

## Immutable calculation

Use the exact repair2 image
`us-central1-docker.pkg.dev/nfl-predictions-503414/nfl-dfs/nfl-dfs@sha256:ce03feb739e51aabedd7cea79f46e13a06a097a7f85e9a5817f38184b67f4fcb`
and code `60f296fdad769b30c0bb7334118698f156e462b9`.

This deliberately preserves the original binary pair/triple interaction
auxiliaries. The later continuous-integrality optimization is mathematically
valid but is excluded so it cannot mask or confound the resource repair.

All original MVP protocol, pair-reach amendment, source-repair bindings,
five-seed native books, leave-one-seed-out pricing, top-40 world construction,
candidate enumeration, aggregation and score-free gate remain byte-identical
to repair2.

## Sole treatment

For every cell change only the Cloud Run envelope from one CPU / 4 GiB to four
CPUs / 16 GiB. Four CPUs are the minimum allocation compatible with 16 GiB and
are treated as one inseparable infrastructure treatment. Retain:

- one task and parallelism one;
- zero retries;
- 12-hour timeout;
- the same service account;
- the same command, arguments and environment other than new output identity.

## Mandatory preflight

Do not launch repair3 unless the strictly harvested
`20260816-atlas-cbc-16g-preflight-v1` summary has all of:

- disposition `r0-complete`;
- status `r0-complete`;
- child return code 0 and no terminating signal;
- OOM-kill delta total 0;
- `uses_realized_outcomes=false`, `persists_lineups=false`, and
  `production_change_licensed=false`.

The launcher must validate these fields and bind SHA-256 values for the
preflight manifest, execution ledger, summary and completion receipt. Any
failure or missing/mismatched receipt blocks the full grid.

## New complete population

Create new job/execution/object identities for every cross-product cell:

- seasons: 2023, 2024, 2025;
- weeks: 1 through 18;
- total: 54 cells;
- jobs: `atlas-md-s<season>-w<week>-r3`;
- output prefix:
  `gs://nfl-predictions-503414-raw/research/atlas-matched-diversity-runs/20260816-atlas-matched-diversity-mvp-v1-repair3`.

No repair2 shard or completed result may be reused. Existing local or cloud
repair3 output is a fail-closed launch error.

## Firewall, harvest and decision

The shard calculation remains outcome-free. Do not inspect any partial shard
result. After all 54 exact executions are terminal, invoke only the strict
repair3 finisher. It must require every execution to be terminal successful,
validate the exact image/command/environment/resource envelope and each
score-free shard identity, then aggregate the complete 54-cell population.

The original score-free gate and pair-reach amendment remain authoritative;
no threshold changes are permitted. Whether it passes or fails, the complete
strictly validated aggregate proceeds to the separately frozen historical
exact-80 diagnostic, whose high-tail amendment protects selected crossings at
220, 230 and 240 in addition to the 200/210 rules. The scorer must be rebuilt
and rebound to the exact repair3 execution IDs and aggregate hashes.

Repair3 itself cannot license production. Production remains contingent on the
separate historical tail-first result and standing operational review.

