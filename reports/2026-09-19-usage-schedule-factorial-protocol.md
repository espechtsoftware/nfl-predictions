# Combined usage and live-game inputs: fixed-pool 2×2 sensitivity

Freeze before the combined effects are read. Prior studies found a large usage-only selection shift and a smaller schedule-only shift. Effects may interact; do not add their estimates or choose a new blend from these studies.

Four cases use the same archived 435-player frame, 6,400 candidate pool, projected means, depth/cold-start fields and original incumbent bank: (A) archived usage + benchmark game lines; (B) recovered six-field usage + benchmark lines; (C) archived usage + archived live-frame lines; (D) recovered usage + archived live-frame lines. Exact recovered usage receipt SHA remains `a021f3ae048033b02a974f616b5b9d68524fe731d9f3d89e35d5cb8670c90025`. All other input identities are pinned by the exact replay preflight.

All four cases use **canonical ascending game-id order**, matching the reviewed live-input patch. That differs from the earlier archive/isolated studies' benchmark row order; A is a co-run canonical control, not claimed to reproduce the archived hsim bank or original97 book. Before effects, check A's canonical game table against the original benchmark and C's against the archived live frame; game identity/prior columns must match across all cases. Input differences are confined to the six declared usage fields and the two game-line fields. Use exact patch source`40a9be9` implementation, historical law fitting unchanged and cutoff2026 firewall active.

For each case calibrate once at seed2326, five400-world passes; generate 10,000 selection worlds at2326 and 10,000 final audit worlds at2426 with the SAME case-specific calibration. Select exactly97 using equal-mass archived incumbent + case hsim worlds and the unchanged verified greedy expected-max implementation. No seed, weight, feature, threshold, calibration or selector search.

Evaluate all four fixed books plus the original archived97 book under every case's independent hsim audit bank and the archived incumbent bank. Report expected max, P220 and GLOBAL proxy for prefixes1/10/20/30/40/80/90/97 and all12actual contest blocks; publish the complete cross-law table, overlaps and first-roster identities. Primary diagnostic contrasts under D's audit: D−A (combined re-selection), D−B (schedule addition after usage), D−C (usage addition after live lines). Report paired MonteCarlo SE/normal95%interval for these prefix97 differences. These are conditional simulator sensitivities, not real football efficacy. Incumbent independent audit remains missing; no complete dual-law validation claim.

Preserve all input/game/calibration identities and score-array hashes; no candidate generation, live writes, policy/timer changes, current lineup outcomes or bank990/991 reads. One process/one BLAS thread, 600-second cap, planned arrays below2GiB. Do not use the result to assert that a larger modeled probability establishes a better real-world law. A guard failure is recorded before any amended retry.
