# Engineering decomposition: historical values versus row order

Frozen after the two actual CLI proofs and their descriptive inspection, before the counterfactual model fits below. This is a disclosed follow-up to the surprising joint-world change, not a new NFL efficacy study or a cache-adoption gate.

Use the two exact archived historical caches (host SHA8aaa5daf…72b7 and fresh SHA1911368c…a096), the host proof's exact live frame, the released Week2 TabPFN cache and unchanged lab source2dc116c. Replay the incumbent selection bank at seed2076,10,000worlds,K1, with the same cold-start filling, simulator, marginal remapping, production centering and DST constants. First require bitwise equality to both saved actual-CLI incumbent matrices using each original cache. If either replay fails, stop and diagnose the mechanics before interpreting counterfactuals.

Then fit exactly two additional combinations: fresh historical values in original host key order, and host historical values in fresh key order. Preserve all values and column dtypes when reordering; require unique identical key sets. No candidate generation or selection, new data query, model tuning, performance score or current outcome read. Current-season outcomes remain firewalled; model training uses the same historical data through2025 already used by the CLI.

For all four combinations retain component predictions and final incumbent bank hashes; compare exact equality, changed cells and maximum cell differences. Compare per-player score multisets and mean absolute differences of player-pair correlations among nonconstant rows as descriptive dependence diagnostics. The correlations are not an efficacy target. Report paired contrasts for values at each order and order at each value set, retaining interaction rather than attributing the entire change to either cause by assumption. Results are conditional engineering behavior for this frame and fit; do not generalize to all slates or claim better220+ selection.

This investigation does not delay or modify the approved three-fix release. The actual host cache stays intact; a separately reviewed future cache policy can use the evidence.
