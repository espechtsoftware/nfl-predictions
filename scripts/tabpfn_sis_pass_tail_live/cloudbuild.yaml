steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - scripts/tabpfn_sis_pass_tail_live/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  machineType: E2_HIGHCPU_8
