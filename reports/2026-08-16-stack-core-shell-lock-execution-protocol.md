# Stack-core x shell production-lock execution protocol

Date frozen: 2026-08-16, before any score-free stack-core/shell treatment
result, production-form lock, or stack-core/shell realized score existed.

Protocol ID: `20260816-stack-core-shell-production-lock-execution-v1`.

## Release and outcome boundary

The 54-slate lock grid remains inert unless the strict score-free finisher has
published both the report and completion for
`20260816-stack-core-shell-scorefree-v1`, the report disposition is exactly
`stack-core-shell-shadow-licensed`, and both objects bind the same positive
frozen gate and accepted-execution-ledger SHA-256. Every lock shard independently
downloads and validates those exact report/completion object hashes,
generations and sizes before reconstructing its five source books.

The lock jobs may query only the existing outcome-free candidate/player-world
sources. No actual score, rank, payout, ownership outcome or label-completeness
field is permitted. The complete 54-lock report and strict completion must
exist before the separately frozen historical scorer may query the actual
catalog. A failed score-free gate closes this grid without an outcome query.

## Grid, canary, and resources

Use one exact source commit, immutable image digest and create-only prefix for
all 54 Sunday-main slates in 2023--2025. The first primary is the actual 2023
Week 1 job on the actual command/prefix path. Release the remaining 53 only
after terminal success, exact execution-spec parity and positive output-object
metadata are verified without downloading its contents.

Each job has one task, parallelism one, 4 CPU, 16 GiB, a two-hour timeout, task
retries zero and the project compute service account. A memory, timeout,
signal, solver, nonzero, cancellation, ambiguous or object-bearing failure is
terminal for the population. One unchanged replacement is permitted only for
the literal `Internal error running task` zero-object platform condition under
the already frozen narrow retry law.

## Strict harvest and consequence

The finisher must prove each Cloud job's execution set equals its receipted
primary/replacement set, validate every accepted execution and all 54 object
generations/sizes before downloading any shard, and then independently verify:

- the exact 54-slate/270-source-artifact grid;
- all candidate and exact-80 roster identities;
- exact 32/128 component libraries, 256 beam and 40 sequential
  pair-expanding proposals;
- proposal admission identity, simulated thresholds, structure and effective
  rank; and
- the score-free report/completion and accepted ledger that licensed the grid.

Only a complete positive lock report may publish
`historical_scoring_licensed=true`. That flag licenses the one already frozen
realized-score diagnostic and no production/UI change. No component count,
source, selector, resource, retry class or roster may change after the canary
or any lock object exists.
