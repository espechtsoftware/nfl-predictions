# Reconciliation: mechanism queue verification

Date: 2026-08-14. This reconciles
`reports/2026-08-14-mechanism-queue-verification.md` against the frozen
protocols, current handoff, implementation and live execution ledger. No
partial lineup outcome was read.

## Accepted

- G3, the registered SIS team-defense schema estimand, current-stack Route
  marginal, Route I1 rank/dependence and fixed midpoint Route R2 all have
  terminal frozen dispositions.
- The SIS schema qualifier is binding. The failed team/game export closes only
  coverage-snap-normalized efficiency from that export. `Att`-composition ASOE
  and player/defender-grain mechanisms are distinct; Phase S subsequently
  selected ASOE.
- The Route evidence covers both registered current-stack insertion points.
  Marginal Route fields failed the served-tail gate. The unshrunk and fixed
  midpoint-shrunk rank/copula treatments failed their dependence gates, with
  R2 strongly harmful while preserving exact control marginals.
- Pass-tail is the only active historical score experiment. Sequential
  harvesting before another outcome-bearing analysis remains the correct
  firewall.

## Scope and terminology corrections

The Route evidence closes the frozen current-stack queue, not every conceivable
Route interaction, transform or future model family. It licenses no further
historical Route score arm. The 2026 prospective shadow remains open.

The phrase "multi-seed I2" conflates two experiments:

- I2 is the conditional Route-marginal x ASOE factorial. Its frozen condition
  required marginal cell `M` to pass. `M` failed, so I2 is terminally not run.
- The artifact-only multi-seed candidate/world factorial is a separate frozen
  experiment. It remains not-yet-launched for the Phase S-selected treatment
  law and follows pass-tail harvest before final forensic closure.

The capacity rationale is directionally correct for another replay panel, but
not universal. Ten active pass-tail cells request 80 vCPU and 320 GiB; the
pending artifact-only analyzer is one 8-vCPU/32-GiB job and fits the nominal
200-vCPU/400-GiB regional limits. It is kept sequential because of the
one-active-historical-score-experiment outcome firewall, not because quota
alone proves it cannot run concurrently.

## Ledger durability repair

The review correctly found that newly released pass-tail mappings could exist
only in the working tree. The launcher now commits and pushes its execution
ledger immediately after each provenance-verified release and before the next
allocation. Because the active Bash process predated that change, its remaining
batches were manually checkpointed and pushed.

All 30 registered mappings are now tracked. The final cell is treatment
R4/2025 execution `replay-sisptt4-2025-ph97f`. Editing the launcher source while
the old process was still reading its final loop caused that controller to exit
with a mixed-source parse error after it had appended and provenance-verified
cell 30. This was an orchestration mistake, not a Cloud Run execution failure;
the current tracked launcher passes `bash -n`, and the immutable panel must not
be relaunched.

At this reconciliation checkpoint the authoritative per-execution poll reports
20 terminal successes, 10 active executions and zero failures. Continue
status-only polling. After all 30 are terminal `True`, use the independent
provenance-verifying finisher, harvest the complete pass-tail result, and only
then launch the separate multi-seed candidate/world analyzer.
