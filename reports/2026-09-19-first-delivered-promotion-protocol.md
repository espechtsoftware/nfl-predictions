# One fixed first-delivered ordering diagnostic

Exploratory follow-up to the observed ordering issue, frozen before the new audit. The motivating first-lineup difference has already been inspected, so this is not an untouched historical adoption test. No current scoring outcomes or bank991 are inputs.

Use the exact ordinary/full delivered books from the actual-host-input D1600 transfer, the same source frame/candidate pool and map, and the original selection banks/masks. Apply only the already committed rule `2026-09-19-first-delivered-promotion.py` (SHA256 `36ffcbcedc9b1b46d5aea5c9d04b425f0b842e3569a53c20802ef39f36af959f`): among clean/soft rows already within delivered first30, promote the largest standalone mean under that book's own equal-mixture selection bank, with earliest delivered-rank ties. Preserve all other relative order. The first30 boundary already exists in production vetting; it is not chosen from audit results. Evaluate the ordinary and full book separately, retaining a no-op if already ordered that way.

Authenticate and reproduce the existing ordinary/full selections before promotion. Derive clean/soft flags from the unchanged vetter's original receipts. Freeze the four books before any new audit computation. Membership and the first30 set must be identical within each comparison, and positions31–97 untouched. Assert exact audit invariance of every prefix of30or more and every block starting after30.

Fresh incumbent audit seed **16260919**, hsim **17260919**, participation masks **20260919058**. Copy the exact already replay-proved actual-host audit constructor from `2026-09-19-participation-host-transfer-v2.py` (SHA256 `b2a4dad35eb98989a880ccdc60ff71ee958e6934d4c5141aa07c5c5ee9c70b38`), changing only the two fresh audit seeds. Reproduce both original actual selection banks before fresh draws. Preserve the exact historical cache, fitted components, hsim calibration seed2326, original game inputs and outcome firewall. No provider query or new candidate generation.

Report every prior prefix/block and both components/equal mixture under full participation and all-active assumptions: expected maximum, P220, fixed historical winner proxy and mean lineup, with paired Monte Carlo intervals. The target is improving first-entry ordering, not improving whole-book coverage. Report any transferred cost to ranks2–24 or another affected region. Do not vary the rule, prefix, thresholds or tie breaks based on the result. No live contest mapping or entry change follows automatically.
