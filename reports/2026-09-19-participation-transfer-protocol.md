# Participation-aware selection: fixed current-book transfer test

Frozen September 19, before the comparison below. This is one conditional simulation experiment under Erich's focused scoring request, not a new historical outcome study or adoption decision.

The current larger-book route has no participation-mixture caller. PREREG-054's independently reproduced historical result remains positive for its winner-CDF proxy and general selection, but its 220+ weeks fell from one to zero. An authenticated September 10 production package contains a deterministic K80 rehearsal and a 2026 map trained on 2022–25. Neither proves current D12800/K97 delivery. The peer's claim that production has no implementation or past rehearsal is incorrect; whether a particular prior book was entered is a separate provenance question.

## Fixed inputs and contrast

- Use only the already frozen repaired D1600 pool and its incumbent/hsim 10,000-world selection and independent audit banks. Retain original candidate order and float32 roster summation. No generation, model fitting or new football labels.
- Current inputs are captured at **2026-09-19T11:50:49.748032Z** using BigQuery time travel. Latest injury snapshot: 10:03:45.895940Z; DK capture: 11:41:50.372859Z; exact group153428. Raw DK `week` is NULL, so the initial week-filtered capture is empty and retained. A separate disclosed repair captures by group and season and asserts September20 Sunday-main game times.
- The 312 candidate players have complete DK matches. Eleven carry Questionable/Doubtful designations, concordant between DK and the injury snapshot. Injury `date_modified` is NULL throughout. Collector time proves prelock observation, not the provider's original publication time; this is disclosed and is not a claim to satisfy every original live-feed gate.
- Use the existing map file SHA `fc5a22bb9c3cab895e8260ef00caca4c389888f4110ceff0b21a59b97b005cbb`, internal map SHA `6f8603105dd83fe995c7c595b956eccfe216e4d6d18be2e97db6bf404dc667a6`. No current fit, smoothing change or probability tuning.
- Normalize explicit raw practice strings to 0/1/2; preserve raw fields and collector times. Unknown statuses/practices, duplicate identities or missing DK records fail. Combine source designations using maximum explicit severity; receipt any disagreement. Exclude candidates containing newly Out/IR/ineligible players from **both** arms. Q/D remain eligible. Absence from the complete injury capture plus healthy DK means no designated availability intervention.
- Control: ordinary equal-component expected-max K97. Treatment: the same selector after independently zeroing each designated player's score in each world with probability `1-p_active`. No redistribution, recentering, hard Q/D exclusion or candidate change. Selection mask seed **20260919054**; audit mask seed **20260920054**. One mask draw per player per world is shared across all lineups containing that player.
- Evaluate both books under both the all-active and participation-mixture audit laws, separately by component and in equal mixture. Report all results; no choice of the audit law after seeing them. The base audit banks have been inspected in earlier experiments; only the availability masks are fresh. Monte Carlo intervals describe these fixed beliefs, not NFL predictive validity.
- Replay the **unchanged actual vetting helper** against the same captured three query results and the current DK statuses, with no new cloud reads. Report raw and delivered order, preserving all97 entries, and prove exact CSV permutation. These are research books, not entry files.

## Outputs and interpretation

Report expected maximum, P(220+), the already frozen global winner-CDF proxy, mean lineup score and model-implied inactive contamination for first1, prefixes10/20/30/40/80/90/97 and all current contest blocks. Show candidate/book overlap, per-player exposures, map classes, first-lineup changes and eligibility removals. Record expected-max gains against the ordinary book before and after vetting so ordering cannot masquerade as a whole-book membership gain.

Mechanics gates: shared pool and exact97 unique legal rosters; deterministic mask replay; all-active identity; control reproduction when eligibility is unchanged; independent selection/audit masks; immutable input hashes; no unknown normalization; no cloud writes or outcome access. Run a synthetic full selection-and-vetting path and real-input support smoke before freezing the numerical read.

A result can justify a concrete recommendation and a separate current-dose rehearsal. It cannot establish more real 220+ lineups. A large all-active sacrifice, contradictory component results or a benefit erased by delivery is a material tradeoff. No automatic live adoption, extra probability sweep, or opening bank991 follows this test. The current-source fit explicitly uses active training rows for production components; market blending and marginal shaping mean this still needs a documented double-counting limitation rather than a claim that all baseline forecasts are perfectly conditional on participation.
