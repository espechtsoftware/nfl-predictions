# PIT-clean TabPFN team-QB exact-80 addendum

Frozen 2026-08-12 before the team-QB side table, either cache, any team-QB
prediction, final-served metric, candidate, or lineup score exists. It applies
the operator's program-wide tail-first decision law to the already-frozen
team-QB-quality protocol.

## Terminal inheritance and branching

- Both cache arms inherit the exact terminal active-label and SCHED feature
  laws. Their separately generated control must reproduce that terminal cache
  on every key and prediction within `1e-10` before any gate is eligible.
- A failed cache validation or final-served active RB/WR/TE Brier-30 gate
  closes this feature without lineup generation and retains the terminal SCHED
  cache law.
- A passing final-served gate licenses exactly one paired exact-80 comparison
  using panels
  `20260812-pitclean-e80-selected-tabpfn-team-qb-control-v1` and
  `20260812-pitclean-e80-selected-tabpfn-team-qb-treatment-v1`.
- An exact-80 loss or tie retains the terminal SCHED feature/cache law. An
  exact-80 win selects the treatment's one-column team-QB broadcast for later
  preseason handoff and prospective deployment work. Neither result directly
  mutates production.

## Common law and only difference

Both books generate 2023--2025 and splice the unchanged terminal Tier-1
2019/2021/2022 history into their 107-slate comparison. They share terminal
K1, direct-role, usage, active-label and SCHED laws, exact-80 selection,
candidate budget, seeds, market blend and optimizer settings. Each arm uses
its own unrounded walk-forward position-factor schedule from the one passing
team-QB final-served report.

Control uses `tabpfn_team_qb_control_v1`; treatment uses
`tabpfn_team_qb_treatment_v1`. The treatment cache appends only the strictly
prior, pass-catcher-only `team_qb_cpoe_l6` feature. The comparator must prove
exact player keys, invariant point-in-time inputs, actuals, seeds and common
levers; only registered distribution-derived outputs may differ. Candidate
means/membership and selected lineups must change, and every completed slate
must contain 80 distinct legal selections.

## Decision

Compare full-book selected weekly maxima in order
`240,230,220,210,200,194,187`. The first nonzero treatment-minus-control count
decides. If all seven counts tie, mean weekly maximum decides; an exact tie
retains the incumbent. Season signs and average lineup scores are diagnostics
only.
