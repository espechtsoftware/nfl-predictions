# ATLAS CBC 16 GiB resource-envelope preflight protocol

Date frozen: 2026-08-16, while both 4 GiB Week 15 diagnostics remained
nonterminal and before any artifact from either diagnostic set was opened.
Protocol ID: `20260816-atlas-cbc-16g-preflight-v1`

## Question

Can the exact old-binary R0 construction for the hardest observed cell, 2024
Week 15, complete in the proposed repair3 resource envelope of four CPUs and
16 GiB?

This is an independent resource-envelope preflight. It does not alter,
supersede, shorten or interpret either frozen 4 GiB diagnostic. It is launched
prospectively so the proposed repair can be tested while those controls finish.

## Fixed source, cell and calculation

Use immutable repair2 image
`sha256:ce03feb739e51aabedd7cea79f46e13a06a097a7f85e9a5817f38184b67f4fcb`
and source `60f296fdad769b30c0bb7334118698f156e462b9`. Inject byte-for-byte source
`scripts/run_atlas_cbc_resource_diagnostic.py` and change only its diagnostic
protocol ID, diagnostic-only output prefix and allowed-cell set before invoking
`main()`.

Run only 2024 Week 15 and the exact R0 prefix specified by the cgroup resource
diagnostic: the identical five native books and player catalog, native
interaction pricing, exact top-40 worlds, structural clustering and 40-lineup
matched-diversity enumeration with an empty prior-ATLAS set. Objective, floors,
stack rules, ordering, tolerance, interaction integrality, packaged CBC binary
and solver options remain unchanged.

## Sole treatment and execution envelope

The sole solver treatment is the Cloud Run resource allocation:

- CPU: 4;
- memory: 16 GiB;
- tasks/parallelism: 1/1;
- retries: 0;
- timeout: 12 hours;
- service account: unchanged from repair2.

The CPU change is the minimum Cloud Run allocation compatible with 16 GiB and
is part of the inseparable resource envelope. It is not a model or scoring
change.

## Firewall and harvest

Use the same child return-code, signal and cgroup instrumentation as the frozen
resource diagnostic. Persist only a mechanical success/failure receipt and,
on failure, the CBC log and MPS. Discard all returned lineups in memory. No
outcome, lineup, candidate summary, selector output, score, effect, gate or
tail probability may be printed or persisted.

Do not inspect the task artifact before the execution is terminal. Invoke only
the strict preflight finisher, which must validate the exact injected source,
execution identity, resource envelope, receipt, cgroup fields and firewall.

## Frozen interpretation

- A validated `r0-complete` receipt with child return code 0 and no OOM-kill
  increment establishes that the proposed 4-CPU/16-GiB envelope mechanically
  clears this fixed hard cell.
- A positive OOM-kill increment proves that even 16 GiB is inadequate for this
  cell. Any other failure is mechanically recorded but does not identify its
  cause by itself.
- Success here does not by itself prove the 4 GiB failures were OOM. That causal
  conclusion remains governed by the independent three-cell cgroup diagnostic.
- This preflight cannot license an ATLAS result, historical score conclusion or
  production change. A resource-supported repair3 must still use new identities
  and rerun all 54 cells before any scoring stage.

