# PIT-clean active-label exact-80 addendum

Frozen 2026-08-12 before the repaired v2 active-label final-served result and
before generating or querying any v2 active-label candidate or lineup score.
It mechanically updates the blocked v1 protocol with repaired identities and
the operator's already-adopted high-score-first decision law.

## Licensing branches

- A failed v2 final-served gate closes the active-only-label arm without a
  lineup launch; retain canonical current-label TabPFN.
- A passing v2 final-served gate licenses exactly one same-image exact-80
  control/treatment pair. Its fixed IDs are
  `20260812-pitclean-e80-selected-tabpfn-current-v2` and
  `20260812-pitclean-e80-selected-tabpfn-active-v2`.
- An exact-80 loss or exact tie retains canonical current-label TabPFN. An
  exact-80 win selects active-only labels for the later SCHED/graph branch and
  licenses a separately reviewed canonical-cache regeneration; research
  tables never become production implicitly.

## Mechanically inherited common law

- Use generation digest `ad50fe19...`, code `a12ab31`, the terminal repaired
  full Tier-1 panel, and K1.
- Inherit direct-role selection from `selected_tier1.txt` and the exact
  terminal multinomial/finite-K law from `selected_usage.txt`.
- Generate only 2023--2025 and splice unchanged 2019/2021/2022 selected-source
  history into both 107-slate books.
- Both books use exact 80 entries, CE 0, boom 40, Gumbel 0, default candidate
  multiple 2 and frozen seeds. Direct-role 12 and its six features/seed are
  present if and only if the Tier-1 role branch was selected.

The control reads `tabpfn_active_label_control_v2`; the treatment reads
`tabpfn_active_label_treatment_v2`. Each season uses its arm's unrounded
position-factor schedule from the sole v2 final-served report. Those cache
and schedule pairs are the only causal differences.

## Mechanical comparison

The comparator must verify the final-served report and cache-validation
hashes, passing gate, 52,307 equal target keys, terminal usage/panel identity,
one schedule per arm/season, exact common levers/seeds/actuals/invariant
point-in-time inputs, and exactly the seven registered distribution-derived
fields as allowable player-output changes. At least one served distribution
and candidate mean/membership/selection must change. Every slate must contain
exactly 80 distinct legal selected lineups with complete labels.

For the full books, compare selected weekly maxima in order
`240,230,220,210,200,194,187`. The first nonzero treatment-minus-control count
decides. If all seven counts tie, mean weekly maximum decides; an exact tie
retains current labels. Season signs and average lineup scores are diagnostic
only. This seven-count/mean rule is the repaired program-wide operator law
frozen for Tier 1 and usage before their results; it supersedes the v1
protocol's five-count neutral rule without using any v2 active-label score.
