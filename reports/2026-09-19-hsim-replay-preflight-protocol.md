# Hsim replay preflight before repaired-input sensitivity

Frozen before any new simulation, September 19. Re-run the original Thursday Week2 D6400 hsim bank using archived frame, season 2026/week2, seed2326, 10,000 worlds, five default 400-world calibration iterations, exact lab source e7255e98bf87297452befb61fb508ad4b368b59f. This is a mechanical replay, not a new model/selector experiment. Do not change calibration, clipping, activity rules, served means or player order.

Authenticate the source bytes of all hsim modules plus data.py against that Git commit. Authenticate six local historical benchmark files by manifest path, bytes and CRC32C; record SHA-256. Restrict the loader to these files and the three permitted table names: player_week_training, raw_weekly_stats and raw_schedules. Use the existing outcome firewall at cutoff2026; current/future realized outcomes never enter a fit. Authenticate frame and archived hsim bank by the existing archive manifest generation and SHA-256. Frame projection includes only identity/role/hsim input columns, no actuals.

Compare the reconstructed float32 player-world array with the archived one: exact equality, differing element count, maximum absolute difference, mean absolute difference, per-player mean-delta maximum, shape and finite values. Save replay array and a JSON receipt with input identities, source bytes, Python/NumPy/pandas versions and runtime. Do not interpret a replay mismatch as an input-repair effect. Investigate any mismatch before comparing corrected features; a co-run baseline can be reported separately if the archived host cannot be reproduced.

One local process, one BLAS thread, 420-second timeout and planned <2GiB arrays. No candidate generation, objective tuning, current lineup outcomes, live dataset writes, policy changes or bank990/991 reads. This preflight does not establish real-world calibration or improved 220+ results. The subsequent repaired-input diagnostic requires a separate committed design and fixed scratch input identity.
