# Measure the selection tradeoff on the completed operating corpus

Risk class S research; no automatic adoption. Earliest use is a reviewed Week-2 ordering/selection trial if the concrete result supports one and the operator adopts it; otherwise Week 3. Current NFL outcomes remain unopened. This is a new post-inspection diagnostic motivated by the earlier all-component rejection screen, not a new claim from that closed experiment.

The completed D6400 archive is available at the identity in [the download receipt](reviews/evidence/2026-09-19-d6400-archive-download.json): 6,399 unique candidates, K97, source `2dc116c`. Its frame and both 429×10,000 selection banks are byte-identical to the already traced morning D160 inputs; the pool is larger. Verify the original greedy source book exactly before applying fresh eligibility or any proposed change. A separate fresh v4.3 rehearsal supplies the current admitted delivered control; retain its status capture and all derived identities. Do not label a merely original raw book as the current final book.

The [proposal function](reviews/evidence/2026-09-19-selection-loss-frontier.py) uses only the two equal-mass selection banks. Eligible candidates must satisfy the same current role/status and house-legality contract as the control. Rank unused candidates by how many currently uncovered worlds they clear at 220, then standalone mean, then candidate index; retain the first128. Evaluate each shortlisted candidate replacing each of the97control positions. Preserve the other delivery positions. This is a bounded **one-exchange** search, not an exhaustive search of books.

For each fixed expected-maximum loss allowance **0, 0.25, 0.5, 1, 2 points**, choose the proposal with largest strictly positive P220 gain, breaking ties by smaller Emax loss, incoming candidate index, then displaced position. A budget without a positive proposal retains the control. Keep all evaluated costs, not just the chosen proposals. No per-component or per-prefix dominance veto is applied. Report their losses explicitly.

The five behavioral tests compare every proposed exchange against direct complete-book recomputation, cover ties/eligibility and distinguish a P220 gain with a visible expected-score cost from a nonexistent gain. A synthetic production-size smoke evaluated 12,416 exchanges on 6,400×20,000 totals at K97 in0.89seconds, peak RSS1,013MiB. No football data was used in that capacity test. Full-data eligibility/support and consumer wiring still precede numerical execution.

Freeze every proposed book before constructing fresh audit worlds. Reconstruct both archived selection banks exactly first. Use fresh incumbent/hsim event seeds **18260919 / 19260919**, 10,000 worlds each, and separate availability seed **20260919061** for the existing fixed participation map. Report both raw and participation audit laws, components and equal mixture; expected maximum, P220, the unchanged labelled winner-score proxy, first entry, existing prefixes and actual contest blocks. Reuse the existing proper-score/uncertainty methods; do not select a budget using these audit results and call that selection independently validated. Any recommendation is an explicit tradeoff, not a hidden winner pick.

The already implemented participation and first-delivered-order rules are separate comparisons on the same authenticated corpus and current eligibility. They do not authorize changing this frontier's shortlist, budgets or one-exchange scope. The first-delivered rule preserves membership and the first30 set. Before audit, supplement this protocol with exact current control/status/derived-book identities and freeze the executable consumer; distinguish readiness from a completed numerical result. Production owns the operational host chain; these outputs remain research artifacts.
